/**
 * Schedules bootstrap
 * @param {Object} params
 * @param {import('cron').CronJob} params.CronJob
 * @param {import('got').Got} params.StrabotManager
 * @param {import('telegraf').Telegraf} params.bot
 */
export async function schedules({ CronJob, StrabotManager, bot }) {
}
